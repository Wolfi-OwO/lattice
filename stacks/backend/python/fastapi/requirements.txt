# Floors with major-version caps: exact pins rot fast and break on newer
# Pythons. Generate a real lockfile for deploys (pip-compile / uv lock).
fastapi>=0.112,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
psycopg[binary]>=3.2,<4.0
email-validator>=2.2,<3.0

# bcrypt and PyJWT directly, NOT passlib/python-jose: both of those are
# unmaintained, and passlib breaks outright against bcrypt >= 4.1.
bcrypt>=4.2,<6.0
pyjwt>=2.9,<3.0
