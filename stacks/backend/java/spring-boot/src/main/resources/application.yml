spring:
  application:
    name: {{projectName}}

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/{{projectName}}}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}

  jpa:
    # Flyway owns the schema. `validate` makes a drifted entity fail at boot
    # instead of silently mutating the database.
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${PORT:{{port}}}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

app:
  jwt:
    # Override in every real environment. HS256 needs >= 32 bytes.
    secret: ${JWT_SECRET:change-me-to-a-long-random-string-at-least-32-bytes}
    expiry: ${JWT_EXPIRY:1h}

logging:
  level:
    {{javaPackage}}: DEBUG
