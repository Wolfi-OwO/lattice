# Reference

- **[CLI reference](cli.md)** — every flag, every subcommand, what each one
  refuses, and what happens when you leave it out.

Two more references live in the repository and are mirrored into
[About the project](../project/conventions.md):

- **[Conventions](../project/conventions.md)** — the contract every built-in
  stack is held to. The same `users` resource, the same error envelope, the same
  shutdown behaviour, in every language.
- **[Why the templates are shaped this way](../project/structure.md)** — the
  reasoning behind the layout, grounded in an audit of real projects rather than
  in taste.
